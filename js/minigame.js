/*let dot = document.getElementById("dotty");
let hurdle = document.getElementById("")

function jump() {

 if(dot.classList != "animated") {
 dot.classList.add("animated");
 }
 setTimeout(function(){
  dot.classList.remove("animated")
 }, 500)
}

let checkfail = setInterval(function() {
   let botTop =
 parseInt(window.getComputerStyle(dot).getPropertyValue("top"))


let hurdleLeft =
 parseInt(window.getComputerStyle(hurdle).getPropertyValue("left"))
if(hurdleLeft < 20 && hurdleLeft > 0 && botTop >= 130) {
 hurdle.style.animation = "none"
 hurdle.style.display = "none"
 alert("Minigame Over")
}
},10)

jump.addEventListener("click", jump()) */